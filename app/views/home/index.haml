.jumbotron
  %h2 An efficient way to manage your comic collection.

#search
  #search-container
  #results-container

- content_for :footer do
  :coffeescript
    $ ->
      search = new MMC.Components.Search()
      React.render search.render(), document.getElementById('search')